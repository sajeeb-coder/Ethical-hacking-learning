# Bandit / Level 1 → Level 2 ✅

**Objective:**  
Read a file that starts with a dash (`-`).

**What I Learned:**  
- Files beginning with `-` are treated as options by commands  
- Using `./` allows reading such files safely

**Command Used:**  
```bash
cat ./-

**
“It was simple, yet tricky enough to test attention to detail.” 
